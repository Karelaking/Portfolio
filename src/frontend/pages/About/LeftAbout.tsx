import { screenSizeConstants } from "../../constants/SizeConstants";

const LeftAbout = () =>  {
  return <div className={`${screenSizeConstants.subScreenSize}`}>LeftAbout</div>;
}

export default LeftAbout
