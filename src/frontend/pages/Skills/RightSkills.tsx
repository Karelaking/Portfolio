import { screenSizeConstants } from "../../constants/SizeConstants";

const RightSkills = () => {
  return <div className={`${screenSizeConstants.subScreenSize}`}>RightSkills</div>;
}

export default RightSkills;
