import { screenSizeConstants } from "../../constants/SizeConstants";

const Projects = () => {
  return <section className={`${screenSizeConstants.mainScreenSize}`}>this is projects section</section>;
}

export default Projects
